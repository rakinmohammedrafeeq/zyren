# -------------------------------------------------------------------
# NOTE:
# For Local Docker Testing (Oracle):
#   → Uncomment Oracle section and comment PostgreSQL section
#
# For Render Hosting (PostgreSQL):
#   → Use PostgreSQL section below
# -------------------------------------------------------------------

spring:
  application:
    name: Zyren

  main:
    lazy-initialization: true

  security:
    oauth2:
      client:
        registration:
          google:
            client-id: ${GOOGLE_CLIENT_ID}
            client-secret: ${GOOGLE_CLIENT_SECRET}
            scope:
              - email
              - profile
            redirect-uri: "{baseUrl}/login/oauth2/code/{registrationId}"

        provider:
          google:
            authorization-uri: https://accounts.google.com/o/oauth2/auth
            token-uri: https://oauth2.googleapis.com/token
            user-info-uri: https://www.googleapis.com/oauth2/v3/userinfo
            user-name-attribute: sub

  datasource:
    url: ${SPRING_DATASOURCE_URL}

    ### Use this block when running Oracle locally with Docker ###
#    driver-class-name: oracle.jdbc.OracleDriver

    ### Use this block for Render (PostgreSQL) ###
    driver-class-name: org.postgresql.Driver

    username: ${SPRING_DATASOURCE_USERNAME}
    password: ${SPRING_DATASOURCE_PASSWORD}

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        ### Use this block when running Oracle locally with Docker ###
#        dialect: org.hibernate.dialect.OracleDialect

        ### Use this block for Render (PostgreSQL) ###
        dialect: org.hibernate.dialect.PostgreSQLDialect

  servlet:
    multipart:
      max-file-size: 20MB
      max-request-size: 20MB

#  mail:
#    host: ${SPRING_MAIL_HOST}
#    port: ${SPRING_MAIL_PORT}
#    username: ${MAIL_USERNAME}
#    password: ${MAIL_PASSWORD}
#    properties:
#      mail:
#        smtp:
#          auth: true
#          starttls:
#            enable: true

resend:
  api:
    key: ${RESEND_API_KEY}

cloudinary:
  cloud-name: ${CLOUDINARY_CLOUD_NAME}
  api-key: ${CLOUDINARY_API_KEY}
  api-secret: ${CLOUDINARY_API_SECRET}

server:
  port: ${PORT:8080}
  address: 0.0.0.0

jwt:
  secret: ${JWT_SECRET}
  expiration: ${JWT_EXPIRATION}

zyren:
  admin:
    email:
      1: ${ZYREN_ADMIN_EMAIL_1}
      2: ${ZYREN_ADMIN_EMAIL_2}
    password:
      1: ${ZYREN_ADMIN_PASSWORD_1}
      2: ${ZYREN_ADMIN_PASSWORD_2}
  mail:
#    from: ${MAIL_FROM}
    to: ${MAIL_TO}

management:
  server:
    port: ${PORT:8080}
  endpoints:
    web:
      base-path: /actuator
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always
  health:
    mail:
      enabled: false
